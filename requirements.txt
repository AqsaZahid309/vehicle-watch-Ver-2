# Web framework
fastapi==0.141.1
uvicorn[standard]==0.53.0

# Database
sqlalchemy==2.0.54
asyncpg==0.31.0
alembic==1.20.0

# Redis
redis==8.1.0

# Validation & settings
pydantic==2.13.5
pydantic-settings==2.15.0
email-validator==2.3.0

# Auth — bcrypt is used directly (passlib is unmaintained and broke on bcrypt>=4.1)
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
python-multipart==0.0.32

# ML
scikit-learn==1.9.1
numpy==2.5.3
joblib==1.6.0

# Gemini AI (google-genai replaces the deprecated google-generativeai SDK)
google-genai==2.25.0

# HTTP client (simulator, notifications, tests)
httpx==0.28.1

# Observability
prometheus-client==0.26.0

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
anyio==4.15.1
# aiosqlite provides async SQLite for the in-memory test DB (no real Postgres needed)
aiosqlite==0.22.1

# Utilities
python-dotenv==1.2.3
